# HW4 dependencies — create a venv and install with:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r HW4/requirements.txt

numpy
pandas
dask[dataframe]
pyarrow
fsspec
s3fs
xgboost
scikit-learn
matplotlib
python-dotenv
